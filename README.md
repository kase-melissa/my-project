# ボーナスストア エントリープランナー

Yahoo!ショッピング店の売上最大化のために、**ボーナスストアPlusにエントリーすべき
日程と自社還元率を、販促カレンダーと実績から利益ベースで提案する** システム。

月次の販促カレンダーを入力すると、ポイント原資の予算内で増分GMVが最大になる
エントリー日程を出力する。見送った日については理由も出す。

---

## 何を解いているか

販促カレンダーには毎日7%から14%までの付与率が並んでいる。しかしそのほとんどは
**モール負担**であって、エントリーしてもしなくても顧客に付く。

自社の意思決定は実質2つしかない。

1. **どの日にボーナスストアPlusに参加するか**
2. **自社設定還元率をいくらにするか**

2026年10月の場合、参加によって**モール負担の上乗せが開くのは6日だけ**
（10/2・7・16・20・27・28）。残りの日は参加しても自社のポイントを配るだけになる。
一方その6日は、自社上乗せ0%でも +2% が開くため原資ほぼゼロで注文が伸びる。

この線引きを、日ごとの期待値計算と予算制約下の最適化で行う。

```
純増効果 = 増分GMV × 粗利率 − 増分ポイント原資 + 新規客のLTV
                               ~~~~~~~~~~~~~~~~
                               自社負担ぶんのみ（モール負担は入らない）
```

---

## クイックスタート

依存は Python 3.11+ と PyYAML のみ。

```bash
pip install pyyaml

# 1. 販促カレンダーの検証と締切一覧
python3 -m bonus_planner check --schedule data/promo_schedule/2026-10.yaml

# 2. 実績から行動モデルを校正（--schedule は必須級。理由は後述）
#    ストアクリエイターProのエクスポートをそのまま渡せる（CP932のままでよい）
python3 -m bonus_planner calibrate \
    --history data/sales_history_monthly.csv \
    --schedule data/promo_schedule/2026-10.yaml \
    --today 2026-09-16 --partial-days 14 \
    --out config/behavior_calibrated.yaml

# 3. エントリー日程を提案させる
python3 -m bonus_planner plan \
    --schedule data/promo_schedule/2026-10.yaml \
    --behavior config/behavior_calibrated.yaml \
    --history data/sales_history_monthly.csv \
    --today 2026-09-16 --partial-days 14
```

出力（`out/bonus_store_plan_YYYY-MM.*`）:

| 形式 | 用途 |
|---|---|
| `.md` | レビュー用。前提・感応度・判断根拠・見送り理由つき |
| `.html` | カレンダー表示。共有用 |
| `.csv` | 日別データ。Excel加工用 |
| `.json` | 他システム連携用 |

---

## 設計の要点

### 1. モール負担と自社負担を分ける

施策ごとに `funding: mall | store` を持たせ、**自社負担ぶんだけをコストに計上する**。
これを混ぜると原資が数倍に膨らみ、提案が成立しない。

2026年10月で自社負担なのは、ストアポイント1% と 自社設定率 だけ。

### 2. 集客と転換率を分ける

```
セッション = base_sessions × 暦 × 市場規模係数   ← モールの集客
転換率     = base_cvr × シェア係数               ← 自社の相対的な魅力
注文数     = セッション × 転換率
```

実績ではこの2つが別々に動いている（2025-09→2026-08 でセッション +16%、
転換率 +23%、単価 +23%）。成長の大半は集客ではなく転換率と単価の改善による。
注文数1本で持つとこの構造が潰れ、前方予測を誤る。

### 3. 市場規模とシェアを分ける

| 種類 | 効果 |
|---|---|
| **全ストア対象**（5のつく日など） | 競合も同条件。シェアは動かず、モール全体の来訪者が増える |
| **参加資格つき**（ボーナスストアPlus、プロモパッケージ） | 競合に対する優位。シェアが動く |

この2つを1本の反応関数にまとめると、逓減が効きすぎて
**モールが最も集客している5のつく日を避ける**提案になる。実務感覚と逆で、
モデルの作りに起因する歪みである。

シェアは**比率ではなく絶対差**で見る。顧客にとって2ポイント分の価値は、
基準率が7%でも11%でも同じ金額（2万円の注文なら400円）だから。

### 4. 注文下限と付与上限を分布で積分する

高単価商材ではこの2つが逆方向に効く。下限未満の注文には付かず、
上限で頭打ちになる。注文単価を対数正規分布で近似し、閉形式で期待付与率を出す。

注文明細7,543件から**実測分布をそのまま使う**。このストアは
ユニーク価格が350種しかなく上位10価格で56%を占めるため、
対数正規の近似が成り立たない（注文下限3,000円の該当率を 48% → 80% と過大評価する）。

| 施策 | 表示 | 注文下限 | 該当率 | 実効率 |
|---|---:|---:|---:|---:|
| 5のつく日 | 4% | なし | 100% | 4.00% |
| ボーナスストアPlus | 2% | 3,000円 | **48%** | **1.62%** |
| プレミアムな日曜日 | 5% | 5,000円 | **31%** | 3.31% |
| **モールクーポン** | 1,000円 | 25,000円 | **0.5%** | **0.11%** |

**モールクーポンは実質的に機能しない。**
段階付与（爆買WEEK）は注文ごとに段を判定する。平均単価で段を決めて
全注文に適用すると、最下段に届かない注文にも付与率を掛けてしまう。

### 5. コストは限界差分で見る

ストアポイント1%は参加有無に関わらず毎日かかる。エントリー判断で増えるのは差分だけ。
同時に、参加日の原資は**エントリーしなくても発生した注文にもかかる**（カニバリ）。

### 6. 貪欲法ではなく動的計画法で選ぶ

「見送る or 参加して還元率をひとつ選ぶ」の択一なので multiple-choice knapsack
として解く。費用対効果順に取る貪欲法では
「安い日を2つ取るより高い日を1つ取る方が良い」ケースを取りこぼす。

### 7. 期間一括エントリーと月跨ぎを扱う

爆買WEEK（10/30〜11/1）のように期間通しでしかエントリーできない施策は
期間全体で1単位として評価し、日ごとに抜き差しする実行不能な提案を出さない。

### 8. 前提の感応度を必ず出す

付与率への反応は日次データがないと校正できない。そこで弱気・既定・強気の
3シナリオを並べ、**どの前提でも選ばれる日**と**前提によって採否が変わる日**を
分けて示す。前者はそのまま実行でき、後者は実績で確かめる対象になる。

---

## 構成

```
bonus_planner/
  models.py          Benefit（カレンダー1行）・参加状態・エントリー単位
  schedule.py        販促カレンダーの読み込みと検証
  calendar_rules.py  曜日・給料日サイクル
  distribution.py    注文単価分布（実測／対数正規）と注文下限の分析
  economics.py       期待付与率、自社負担の切り分け
  behavior.py        需要モデル（市場規模 × シェア）
  planner.py         エントリー単位の構築と日次見積り
  optimizer.py       予算制約下の最適化（DP、目的関数切替）
  sensitivity.py     前提を振ったときの結論の振れ幅
  calibrate.py       実績からのモデル校正（月次・日次を自動判別）
  report.py          Markdown / CSV / JSON / HTML 出力
  cli.py             CLI
config/
  config.yaml              自社の経済条件と参加状態
  behavior_priors.yaml     行動モデルの初期仮値
data/
  promo_schedule/2026-10.yaml       2026年10月カレンダーの転記
  sales_history_monthly.csv         実績（ストアクリエイターProのエクスポート）
  order_values.csv                  注文明細の金額列（gitignore）
  order_value_profile.json          注文単価分布の要約統計
  sales_history_monthly_sample.csv  動作確認用の合成データ
docs/
  model.md           モデル仕様（係数を触る前に読む）
  operations.md      月次運用手順
tools/
  make_sample_monthly_history.py  合成データ生成（実データではない）
```

---

## 重要: 何が実測で何が仮値か

月次実績（`month,orders,gmv`）で校正できるのは限られる。
**できないものは仮値のまま残し、レポート冒頭に明記している。**

| 係数 | 月次実績 | 日次実績 | 日次＋エントリー記録 |
|---|:---:|:---:|:---:|
| `base_sessions` | ○ | ○ | ○ |
| `base_cvr` | ○（セッション列が必要） | ○ | ○ |
| 月次季節係数 | △ | ○ | ○ |
| `aov` | ○ | ○ | ○ |
| 曜日係数 | × | ○ | ○ |
| 給料日サイクル係数 | × | ○ | ○ |
| 市場規模・シェアの弾力性 | × | × | ○ |
| 注文単価の分布 | × | × | **注文明細があれば実測（完了）** |

△ = 1ヶ月あたり観測1点のため弱い推定（縮小推定で過学習を抑えている）。

**金額の絶対値は目安として扱うこと。** 日ごとの優先順位づけには使えるが、
増分GMVや純増効果の絶対額は実績での検証が必要。

### `calibrate --schedule` を省略しないこと

実績には販促イベントによる上振れがすでに含まれている。モデルはそこへさらに
係数を掛けるため、補正しないとベースライン予測が系統的に過大になる。

補正は **集客側と転換率側の2系統** が必要で、`--schedule` を渡すと両方が適用される。

| 実績の量 | 押し上げていた施策 | 割る係数 |
|---|---|---|
| セッション | 全ストア対象（5のつく日など） | 市場規模係数の平均 |
| 転換率 | プロモーションパッケージ加入で開く施策 | シェア係数の平均 |

詳細は [docs/model.md](docs/model.md) と [docs/operations.md](docs/operations.md) を参照。

---

## テスト

```bash
python3 -m unittest discover -s tests -t .
```

経済計算・需要モデル・カレンダー転記の検算・最適化・校正・感応度・CLIの
端から端まで、231件。

うち `tests/test_schedule_2026_10.py` は、転記したYAMLから算出した総付与率が
**販促カレンダーPDF最下段の「最大付与率」行と全32日で一致すること**を検証している。
転記ミスに対する回帰テスト。
