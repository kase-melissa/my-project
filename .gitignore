__pycache__/
*.pyc
.venv/

# 生成物(実行のたびに作り直される)
out/
config/behavior_calibrated.yaml
config/store_suggested.yaml

# 注文明細(金額のみ)。粒度が細かいためリポジトリには置かない。
# 代わりに data/order_value_profile.json に要約統計を置く。
data/order_values.csv
